(# comment )
(struct int-pair
 (int a)
 (int b))

(var int-pair obj)
(assign obj (make-int-pair (a (make-int 3)) (b (make-int 4))))

(function fib ((int n)) int
 (if (call <= n (make-int 1))
  (= ret n)
  (= ret
   (call +
    (call fib (call - n (make-int 1)))
    (call fib (call - n (make-int 2)))))))

(call fib (make-int 3))
